CONSTANTS
  Peers = {peer1, peer2}
  FullNodes = {fn1}
  Swarms = {swarm1}
  SessionIDs = {sid1, sid2}
  SDPOffers = {offer1}
  SDPAnswers = {answer1}
  MsgIDs = {msg1, msg2, msg3, msg4, msg5, msg6}
  TTLMax = 3
  NULL = NULL

SPECIFICATION Spec

CONSTRAINT StateConstraint

INVARIANTS
  TypeOK
  Invariant1
  Invariant2
  Invariant_NoDupForward
  Invariant_TTLNonNegative
